spring:
  application:
    name: CONFIG-DEV
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

  instance:
    preferIpAddress: true
    fetch-registry: true
    register-with-eureka: true
